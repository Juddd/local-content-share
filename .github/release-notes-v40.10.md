# Local Content Share v40.10

- 网页文字区新增紧凑的悬浮搜索按钮。
- 支持实时检索文字卡片的标题与完整正文，多个关键词需同时匹配。
- 显示匹配数量和无结果状态，清空或关闭后恢复全部卡片。
- 搜索条件兼容收藏排序、局部新增、修改、删除以及 SSE 实时更新。
- Release 附带 Android `v1.0.69` 正式签名 APK；App 端同样支持离线文字搜索。
