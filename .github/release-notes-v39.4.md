# Local Content Share v39.4

- 新增 `/api/v1/items` JSON 接口，供原生 Android 客户端同步 Snippets、Files 和 Links。
- 增加 Android 16 原生客户端“内容中转”的完整源代码。
- App 支持本地缓存即时启动、局域网优先直连、异地服务器自动切换，并主动使用非 VPN 物理网络。
- App 支持文字、文件、链接、Notepad、上传、下载、复制、编辑、重命名、删除、排序、预览字数及过期时间。

Android 正式签名 APK 由 NAS 本地构建，签名密钥不会提交到 GitHub。
