# Local Content Share 个人增强版 v38-yode.2

## 本次调整

- Snippets、Files 和 Links 统一采用最新条目优先的显示方式
- 推荐部署镜像标签改为 `ghcr.io/juddd/local-content-share:latest`
- 保留 `v38-yode.2` 固定版本标签，便于需要时回滚

本次升级不会修改 `/app/data` 中的现有内容。
